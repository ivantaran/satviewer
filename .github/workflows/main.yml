name: main

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - name: Install Qt
      uses: jurplel/install-qt-action@v2
    - name: qmake
      run: qmake
    - name: make
      run: make
