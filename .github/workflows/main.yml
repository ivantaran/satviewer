name: main

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
      - name: satviewer
        uses: ivantaran/satviewer
      - name: test
        run: ls -lia
